import styled from "styled-components/native";
import theme from "../../theme";
import { scale } from "../../utils/layout";

export const BalanceSummaryView = styled.View`
  width: 100%;
  background-color: ${theme.colorPalette.skyBlue[5]};
  border-radius: 13;
`;
