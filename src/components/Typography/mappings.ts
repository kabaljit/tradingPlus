import theme from '../../theme';
import { scale } from '../../utils/layout';

export const titleSizes = {
  small: scale(17),
  medium: scale(25),
  large: scale(34),
};

export const fontWeight = {
  regular: theme.fontWeights.fw400,
  medium: theme.fontWeights.fw600,
  bold: theme.fontWeights.fw700,
};

export const paragraphSizes = {
  extraSmall: scale(13),
  small: scale(14),
  medium: scale(16),
  large: scale(17),
};
