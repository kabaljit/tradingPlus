export interface SettingsScreenParams {
  // Navigation parameters here.
}

export interface SettingsScreenFormValues {
  // Formik parameters here.
}

export interface SettingsScreenProps extends SettingsScreenParams {
}

export interface SettingsMenu {
  title: string;
  route: string
}