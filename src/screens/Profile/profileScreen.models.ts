export interface ProfileScreenParams {
  // Navigation parameters here.
}

export interface ProfileScreenFormValues {
  // Formik parameters here.
}

export interface ProfileScreenProps extends ProfileScreenParams {}

export interface ProfileItems {
  title: string;
  value: string;
}
