import * as React from 'react';

import { {{ pascalCase name }}Props } from './{{ camelCase name }}.models';
import { {{ pascalCase name }}View } from './{{ camelCase name }}.styles';
import { i18n } from './{{ camelCase name }}.i18n';

export const {{ pascalCase name }}: React.FunctionComponent<{{ pascalCase name }}Props> = ({

}) => {

  return (
    <{{ pascalCase name }}View>

    </{{ pascalCase name }}View>
  );
};
