import React from 'react';
import renderer from 'react-test-renderer';

import { {{ pascalCase name }} } from './{{ camelCase name }}';
import { {{ pascalCase name }}Props } from './{{ camelCase name }}.models';

const props: {{ pascalCase name }}Props = {

};

describe('Given a {{ pascalCase name }} component, when it is rendered', () => {
  it('should match the snapshot', () => {
    const tree = renderer.create(<{{ pascalCase name }} {...props} />).toJSON();
    expect(tree).toMatchSnapshot();
  });
});
