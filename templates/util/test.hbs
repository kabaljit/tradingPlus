import { {{ camelCase name }} } from './{{ camelCase name }}';

describe('given a {{ camelCase name }} function', () => {
  describe('when ', () => {
    it('should ', () => {
      expect().toEqual();
    });
  });
});
