import * as React from 'react';

import { {{ pascalCase name }}ScreenFormValues, {{ pascalCase name }}ScreenProps } from './{{ camelCase name }}Screen.models';
import { i18n } from './{{ camelCase name }}Screen.i18n';

export const {{ pascalCase name }}Screen: React.FunctionComponent<{{ pascalCase name }}ScreenProps> = ({ navigation }) => {

  return (
    <></>
  );
}

export default ({{ pascalCase name}}Screen);
